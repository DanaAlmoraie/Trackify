<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/welcomeRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#333333">


    <ImageView
        android:id="@+id/imgHeroTruck"
        android:layout_width="424dp"
        android:layout_height="480dp"
        android:scaleType="centerCrop"
        android:src="@drawable/truck"
        app:layout_constraintBottom_toTopOf="@id/contentContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/imgOverlay"
        android:layout_width="370dp"
        android:layout_height="262dp"
        android:background="@drawable/gradient_overlay"
        app:layout_constraintBottom_toBottomOf="@id/imgHeroTruck"
        app:layout_constraintEnd_toEndOf="@id/imgHeroTruck"
        app:layout_constraintStart_toStartOf="@id/imgHeroTruck"
        app:layout_constraintTop_toTopOf="@id/imgHeroTruck" />

    <LinearLayout
        android:id="@+id/contentContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="32dp"
        app:layout_constraintTop_toBottomOf="@id/imgHeroTruck"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvWelcomeTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:letterSpacing="0.03"
            android:text="Welcome Trackify Driver"
            android:textColor="@color/pure_white"
            android:textSize="26sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvWelcomeSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:lineSpacingExtra="2dp"
            android:text="Monitor your trucks, follow the best routes, and keep maintenance under control — all in one app."
            android:textColor="@color/light_gray"
            android:textSize="14sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnGetStarted"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="24dp"
            android:backgroundTint="@color/orange"
            android:text="I'm new – Create account"
            android:textAllCaps="false"
            android:textColor="@color/pure_white"
            app:cornerRadius="24dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAlreadyHaveAccount"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="12dp"
            android:backgroundTint="@android:color/transparent"
            android:text="I already have an account"
            android:textAllCaps="false"
            android:textColor="@color/orange"
            app:cornerRadius="24dp"
            app:strokeColor="@color/orange"
            app:strokeWidth="1dp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
